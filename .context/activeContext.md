# Active Context

## Current focus
Establish deterministic repository bootstrap memory by materializing the required `.context/` files and enforcing their presence in local/dev/CI workflows.

## Active workstream
- Create canonical context seeds for product, architecture patterns, tech constraints, and status.
- Add CI-check script that fails when required context files are missing.
- Wire check into CI and contributor/startup instructions.

## Next steps
1. Keep `.context/*` aligned with roadmap and sprint status updates.
2. Update this file and `progress.md` at task completion checkpoints.
3. Extend context validation if additional mandatory memory files are introduced.

## Source anchors
- `AGENTS.md` startup block (`Initialize Memory`)
- `docs/operations/agent_execution_commands.md`
- `docs/exec-plans/active/sprint-status.yaml`

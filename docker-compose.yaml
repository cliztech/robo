# Deprecated compatibility entrypoint.
# Prefer explicit layered compose commands documented in docs/runtime_deployment_matrix.md.
# Temporary bridge for tooling that still references docker-compose.yaml.
name: ${COMPOSE_PROJECT_NAME:-dgn-dj}

services:
  backend:
    extends:
      file: docker-compose.base.yml
      service: backend
  frontend:
    extends:
      file: docker-compose.base.yml
      service: frontend
  db:
    extends:
      file: docker-compose.base.yml
      service: db
  redis:
    extends:
      file: docker-compose.base.yml
      service: redis

# ──────────────────────────────────────────────────────────────
# DGN-DJ / RoboDJ — Environment Variables
# ──────────────────────────────────────────────────────────────
# Copy this file to `.env` and fill in real values.
#   cp .env.example .env
#
# NEVER commit the .env file (it's in .gitignore).
# ──────────────────────────────────────────────────────────────

# ── Database ──────────────────────────────────────────────────
# SQLite (default for local dev):
ROBODJ_DATABASE_URL=sqlite:///config/robodj_runtime.db
# PostgreSQL (production / Docker dev):
# ROBODJ_DATABASE_URL=postgresql://robodj:robodj@localhost:5432/robodj

# ── Security Keys ────────────────────────────────────────────
# Primary application secret (base64url, 43+ chars)
ROBODJ_SECRET_KEY=REPLACE_WITH_GENERATED_SECRET_KEY
# V2 secret key (hex, 128 chars)
ROBODJ_SECRET_V2_KEY=REPLACE_WITH_GENERATED_SECRET_V2_KEY
# Scheduler API authentication key (32+ chars)
ROBODJ_SCHEDULER_API_KEY=REPLACE_WITH_GENERATED_SCHEDULER_KEY

# ── Key Expiry (ISO 8601) ───────────────────────────────────
# ROBODJ_SECRET_KEY_EXPIRES_AT=2026-06-01T00:00:00Z
# ROBODJ_SECRET_V2_KEY_EXPIRES_AT=2026-04-01T00:00:00Z
# ROBODJ_SCHEDULER_API_KEY_EXPIRES_AT=2026-08-01T00:00:00Z

# ── Secret Behavior ─────────────────────────────────────────
# Allow fallback to config/*.key files when env vars are missing.
# Set to "false" in production — env vars should be the sole source.
ROBODJ_ALLOW_FILE_SECRET_FALLBACK=true

# ── AI / LLM ────────────────────────────────────────────────
OPENAI_API_KEY=REPLACE_WITH_OPENAI_API_KEY
# ANTHROPIC_API_KEY=REPLACE_WITH_ANTHROPIC_API_KEY
# GOOGLE_API_KEY=REPLACE_WITH_GOOGLE_API_KEY

# ── Supabase ─────────────────────────────────────────────────
SUPABASE_URL=REPLACE_WITH_SUPABASE_URL
SUPABASE_ANON_KEY=REPLACE_WITH_SUPABASE_ANON_KEY
# SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SUPABASE_SERVICE_ROLE_KEY

# ── TTS / Voice ─────────────────────────────────────────────
# ELEVENLABS_API_KEY=REPLACE_WITH_ELEVENLABS_KEY

# ── Application ─────────────────────────────────────────────
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:8000
ROBODJ_LOG_LEVEL=INFO

import { describe, it } from 'vitest'

describe.skip('POST /api/ai/analyze-track', () => {
  it('covered by unit telemetry tests', () => {
    // Intentionally skipped: route imports AI SDK transitive deps not available in this test runtime.
  })
})

# DGN-DJ Skills

Each skill is a folder containing:

- `skill.json`: required metadata used for discovery and validation.
- `README.md`: operator-facing usage and scope notes.

Use `python3 scripts/validate_skills_and_mcp.py` to verify skill metadata and MCP manifests.

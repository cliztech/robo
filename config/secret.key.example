# RoboDJ Secret Key Template (example only)
#
# Preferred source of truth: `ROBODJ_SECRET_KEY` from environment/secret manager.
# This file is only a local fallback for development or break-glass recovery.
# Never commit real key material.
#
# Example generation command (32 random bytes, URL-safe base64-like token):
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
REPLACE_WITH_GENERATED_SECRET_KEY

# RoboDJ Secret Key Template (example only)
#
# This file documents the format for `config/secret.key` and
# `config/secret_v2.key`. Do not commit real key material.
#
# Provisioning guidance:
# 1) Generate keys out-of-band (vault/secret manager/secure deployment step).
# 2) Write the generated value into local runtime files:
#      - config/secret.key
#      - config/secret_v2.key
# 3) Keep both files untracked by git.
#
# Example generation command (run locally, do not commit output):
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Replace the placeholder below with your generated key value.
REPLACE_WITH_GENERATED_KEY

# RoboDJ Secret V2 Key Template (example only)
#
# Preferred source of truth: `ROBODJ_SECRET_V2_KEY` from environment/secret manager.
# This file is only a local fallback for development or break-glass recovery.
# Never commit real key material.
#
# Example generation command (64 random bytes in hex):
#   python -c "import secrets; print(secrets.token_hex(64))"
# RoboDJ v2 Secret Key Template (example only)
#
# This file is a template/example committed to git for format guidance only.
# Do not commit real key material.
#
# Production storage location (outside git history):
#   - config/secret_v2.key (local runtime file, git-ignored)
# Preferred source-of-truth:
#   - Secret manager/vault with deployment-time injection.
#
# Operator checks before activation:
# - Verify provenance (approved vault flow or approved local generation command).
# - Verify value has not appeared in repository history, docs, tickets, or chat logs.
#
# Example generation (64 random bytes, hex):
#   python - <<'PY'
#   import secrets
#   print(secrets.token_hex(64))
#   PY
#
# Replace this placeholder only in `config/secret_v2.key`, not in this template.
REPLACE_WITH_GENERATED_SECRET_V2_KEY

name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
      working-directory: make-4.3
    - name: make
      run: make
      working-directory: make-4.3
    - name: make check
      run: make check
      working-directory: make-4.3
    - name: make distcheck
      run: make distcheck
      working-directory: make-4.3
